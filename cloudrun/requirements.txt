flask==3.0.0
sqlalchemy==2.0.23
pymysql==1.1.0
gunicorn==21.2.0
argon2-cffi==23.1.0
pyseto==1.1.0
redis==5.0.1
# firebase-admin's `firestore` submodule (used by groups.py to mirror group
# membership for Firestore security rules — see GROUP_PLAN.md Phase 1) is
# backed by google-cloud-firestore, which firebase-admin already pulls in
# transitively — no separate pin needed.
firebase-admin==6.4.0
# Pin cryptography explicitly above firebase-admin's transitive floor so
# ML-KEM-768 hybrid X3DH is available (requires >= 43.0.0).
cryptography==43.0.3
# GCS signed URLs for encrypted media blobs (Phase 1 A/V messaging).
# firebase-admin already pulls this in transitively; explicit pin for clarity.
google-cloud-storage==2.18.0
